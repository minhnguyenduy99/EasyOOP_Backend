import { MenuService } from "src/menu";
import { IntegrationService } from "../integration";

var CacheService = {
    menuService: undefined as MenuService,
    integrationService: undefined as IntegrationService,
}

export default CacheService

